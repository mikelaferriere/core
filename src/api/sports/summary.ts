import { Enums } from '@mikelaferriere/espn-api'
import { ScoringPlay } from "../../types/sports";

export const fetch = (
    league: Enums.League,
    id: string
): ScoringPlay[] => {
    return []
}
